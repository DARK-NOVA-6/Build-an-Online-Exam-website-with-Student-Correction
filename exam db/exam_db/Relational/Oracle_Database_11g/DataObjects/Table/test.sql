CREATE TABLE test (
    id                   INTEGER NOT NULL,
    start_time           DATE NOT NULL,
    test_duration        INTEGER NOT NULL,
    correcting_duration  INTEGER NOT NULL,
    course_id            INTEGER NOT NULL,
    teacher_id           INTEGER NOT NULL
);